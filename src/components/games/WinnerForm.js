import React from 'react'
// import { Link } from 'react-router-dom'

const WinnerForm = ({ guess, wrong, wrongAnswer, newGame, emptyWord, handleSubmit, correctGuess, handleChange, cancelPath }) => (
  <div>
    <h1>You Win!</h1>
  </div>
)

export default WinnerForm
