import React from 'react'
// import { Link } from 'react-router-dom'

const LoserForm = ({ guess, wrong, wrongAnswer, newGame, emptyWord, handleSubmit, correctGuess, handleChange, cancelPath }) => (
  <div>
    <h1>You Lose!</h1>
  </div>
)

export default LoserForm
